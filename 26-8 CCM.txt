COPY
(
select
    partner_number as "Tracking No",
    reff,
    card_name,
    tanggal_order,
    rpx_delivery_date as "Delivery Date", 
    sla,
    state as "Status Pengiriman",
    rpx_retur_status as "Retur Status",
    tanggal_retur,
    rpx_received_by as "Received By", 
    rpx_rcv_relation as "Relation", 
    harga,
    jenis_order ,
    kode_kurir,
    city,
    city_alt,
    site_desc as "Keterangan",
    jml_kirim,
    jml_box,
    addr1,
    addr2,
    addr3,
    zipcode
from bmri_surat
where product_kode='PCM' 
    and DATE_PART('YEAR',tanggal_order)='2024'
    and DATE_PART('MONTH',tanggal_order) in ('05','06','07','08')
)
TO '/home/trackinghubadmin/CEKDATA/20240826_PCM_1.csv' With CSV DELIMITER ',' HEADER;